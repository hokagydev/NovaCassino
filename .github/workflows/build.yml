name: Build NovaCassino

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]
  release:
    types: [published]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3

      # Запускаем сборку с явно заданными флагами сети/TLS для Gradle,
      # чтобы JitPack и сторонние репозитории не рвали TLS-соединение
      - name: Build with Gradle
        run: gradle build -Dhttps.protocols=TLSv1.2,TLSv1.3 --no-daemon

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: NovaCassino-Plugin
          path: build/libs/NovaCassino-*.jar

      - name: Upload Release Asset
        if: github.event_name == 'release'
        uses: softprops/action-gh-release@v1
        with:
          files: build/libs/NovaCassino-*.jar
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
